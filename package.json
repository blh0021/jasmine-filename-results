{
  "name": "jasmine-filename-results",
  "version": "0.0.1",
  "description": "Adds a _specFile object containing filename and line number to jasmine results",
  "main": "lib/jasmine-filename-results.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Harrington",
  "license": "ISC",
  "dependencies": {
    "underscore": "^1.8.3"
  }
}
